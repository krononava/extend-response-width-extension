const responseBlock = document.querySelector('[data-element-id="response-block"]');
if (responseBlock) {
  responseBlock.style.paddingLeft = '0.5rem';
}

const avatarContainer = document.querySelector('[data-element-id="chat-avatar-container"]');
if (avatarContainer) {
  avatarContainer.style.display = 'none';
}
